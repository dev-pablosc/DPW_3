// crear arrays (2)
let miArray = [];
let miArray2 = new Array();

// agregando con el 1er metodo
miArray = [1, 2, 3];

/*
console.log(miArray)

// agregando con el 2do metodo
const dias = new Array ('lunes', 'martes', 'miercoles');
console.log(dias)
*/
// array puede contener datos variados
const variado = ['Hoola', 14, true, null, miArray, {nombre: 'Pablo', lenguaje: 'JS'}]
console.log(variado)

// acceder a los valores
const numeros = [10, 20, 30, 40, ['a', 'e', 'i']]
console.log(numeros)
// accedemos por indice

// valores del array anidado
console.log(numeros[4][2])

// varios indices
console.log(numeros[2], numeros[4])

