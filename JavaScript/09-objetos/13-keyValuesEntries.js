// Metodos de Objetos (3)
const producto = {
    nombre: 'Tv curva',
    precio: 3742,
    disp: false
}
console.log(producto);

// key, llaves
console.log(Object.keys(producto))

// values
console.log(Object.values(producto))

// entries
console.log(Object.entries(producto))