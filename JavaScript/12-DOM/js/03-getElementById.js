const form = document.getElementById('formulario')
console.log(form)

// si no existe ? -> retorna null
const noExiste = document.getElementById('noExiste')
console.log(noExiste)

// no debe existir mas de 2 id con el mismo nombre por pagina